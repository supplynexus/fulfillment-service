---
description: Backend Python/FastAPI development rules
globs: ["app/**/*", "alembic/**/*", "scripts/**/*.py", "tests/**/*.py", "requirements.txt", "pyproject.toml"]
---

# Backend 开发规则

## 技术栈
- **Python 3.11+** + **FastAPI**
- **SQLAlchemy 2.0** (异步)
- **PostgreSQL** + **Redis**
- **Pydantic** 数据验证

## 日志记录模板

```python
from app.core.logging import get_logger

logger = get_logger(__name__)

try:
    logger.info("🔍 开始处理请求", params=params, tenant_id=tenant.id)
    # 业务逻辑
    logger.info("✅ 步骤完成", result=result, count=len(result))
except Exception as e:
    logger.error("❌ 操作失败", error=str(e), tenant_id=tenant.id)
    import traceback
    logger.error("   异常堆栈", stack=traceback.format_exc())
    raise HTTPException(status_code=500, detail=f"详细错误信息: {str(e)}")
```

## 加密数据处理

```python
from app.core.security import decrypt_data

# 1. 接收加密数据
encrypted_access_token = request_data.get('access_token')

# 2. 解密凭据
try:
    access_token = decrypt_data(encrypted_access_token)
    logger.info("✅ 凭据解密成功")
except Exception as e:
    logger.error(f"❌ 凭据解密失败: {str(e)}")
    raise HTTPException(status_code=400, detail="Failed to decrypt credentials")

# 3. 使用解密后的数据
external_service = ExternalService(api_token=access_token)
```

### 测试连接 - 支持明文和加密两种格式

```python
try:
    # 尝试解密（已保存店铺）
    access_token = decrypt_data(encrypted_access_token)
except Exception:
    # 解密失败，使用明文（对话框测试）
    access_token = encrypted_access_token
```

## Hashids / 主键

- 约定与用法见 **api-id-hashid.mdc**。后端：`encode_id(id)` 返回、`decode_id(hashid)` 接收，禁止返回数字主键。

## API 开发流程

1. 先设计后端 API 接口
2. 实现路由和业务逻辑
3. 前端实现调用
4. 测试验证

## 常见错误预防

- **404**：检查路由路径是否匹配
- **405**：检查 HTTP 方法是否正确
- **400**：检查请求参数格式
- **401**：检查签名和鉴权
- **500**：检查异常处理和日志

## 日志与调试（合并自 logging）

- 出错时**先看日志**再改代码；关键步骤记录：请求/参数、认证、查询、异常与堆栈。
- 禁止记录敏感数据与主键 ID；鉴权失败时检查签名生成与密钥配置。详见上方日志模板。
