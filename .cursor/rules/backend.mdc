---
alwaysApply: true
---



# Backend 开发环境规则

## 1. 环境配置规则

### 环境变量管理
- **绝对禁止** 在命令行中使用 `export` 或直接在命令前设置环境变量
- 环境变量必须通过 `.env` 文件或环境配置文件设置

## 2. 服务启动规则

### 开发环境启动（推荐方式）
使用三个终端分别启动服务：

#### 终端1: FastAPI Web 服务
```bash
cd backend && source .venv/bin/activate && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### 终端2: Celery Worker
```bash
cd backend && source .venv/bin/activate && celery -A app.tasks.celery_app worker --loglevel=info
```

#### 终端3: Celery Beat
```bash
cd backend && source .venv/bin/activate && celery -A app.tasks.celery_app beat --loglevel=info
```

### 后台启动方式（可选）
```bash
cd backend && source .venv/bin/activate && ./scripts/start_celery.sh
```

### 启动前检查
- 确保在 backend 目录下执行，且虚拟环境已激活
- 如果已经在 backend 目录时，就不要 cd backend 了
- 所有服务都应该后台运行并监控日志

## 3. 数据库操作规则

### 数据库访问优先级
1. **优先使用** MCP postgres-supplynexus-local 工具
2. **备选方案** Docker容器访问
3. **最后选择** Python脚本（避免创建临时测试文件）

### 数据库结构修改流程
**绝对禁止直接执行 SQL DDL 命令**

1. **修改模型文件** - 在 `app/models/` 目录下修改相应的 SQLAlchemy 模型
2. **生成 Alembic 迁移文件** - 执行 `alembic revision --autogenerate -m "描述"`
3. **执行迁移** - 执行 `alembic upgrade head`

### 迁移文件管理
- 迁移文件位于 `migrations/versions/` 目录
- 每次修改数据库结构都必须生成新的迁移文件
- 迁移文件一旦提交到版本控制，不得修改
- 如果需要修改，创建新的迁移文件

## 4. 开发规范

### 错误处理
- 发现错误时立即修复，不要重复错误
- 优先使用三个终端分别启动的方式，便于监控和调试

### 临时文件管理
- **临时测试文件必须放入 `temp/` 目录**，方便事后统一删除


# Backend 开发环境规则

## 1. 环境配置规则

### 环境变量管理
- **绝对禁止** 在命令行中使用 `export` 或直接在命令前设置环境变量
- 环境变量必须通过 `.env` 文件或环境配置文件设置

## 2. 服务启动规则

### 开发环境启动（推荐方式）
使用三个终端分别启动服务：

#### 终端1: FastAPI Web 服务
```bash
cd backend && source .venv/bin/activate && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### 终端2: Celery Worker
```bash
cd backend && source .venv/bin/activate && celery -A app.tasks.celery_app worker --loglevel=info
```

#### 终端3: Celery Beat
```bash
cd backend && source .venv/bin/activate && celery -A app.tasks.celery_app beat --loglevel=info
```

### 后台启动方式（可选）
```bash
cd backend && source .venv/bin/activate && ./scripts/start_celery.sh
```

### 启动前检查
- 确保在 backend 目录下执行，且虚拟环境已激活
- 如果已经在 backend 目录时，就不要 cd backend 了
- 所有服务都应该后台运行并监控日志

## 3. 数据库操作规则

### 数据库访问优先级
1. **优先使用** MCP postgres-supplynexus-local 工具
2. **备选方案** Docker容器访问
3. **最后选择** Python脚本（避免创建临时测试文件）

### 数据库结构修改流程
**绝对禁止直接执行 SQL DDL 命令**

1. **修改模型文件** - 在 `app/models/` 目录下修改相应的 SQLAlchemy 模型
2. **生成 Alembic 迁移文件** - 执行 `alembic revision --autogenerate -m "描述"`
3. **执行迁移** - 执行 `alembic upgrade head`

### 迁移文件管理
- 迁移文件位于 `migrations/versions/` 目录
- 每次修改数据库结构都必须生成新的迁移文件
- 迁移文件一旦提交到版本控制，不得修改
- 如果需要修改，创建新的迁移文件

## 4. 开发规范

### 错误处理
- 发现错误时立即修复，不要重复错误
- 优先使用三个终端分别启动的方式，便于监控和调试

### 临时文件管理
- **临时测试文件必须放入 `temp/` 目录**，方便事后统一删除
