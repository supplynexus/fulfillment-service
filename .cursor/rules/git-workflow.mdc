---
alwaysApply: true
---
## Git 工作流规则

### 禁止直接提交到保护分支
- 禁止直接提交到 `develop` 分支
- 禁止直接提交到 `main` 分支
- 所有更改必须通过 Pull Request 流程

### 正确的 Git 工作流
1. 始终从 `develop` 创建功能分支
2. 在功能分支上进行更改
3. 提交到功能分支
4. 创建 PR 到 `develop`
5. 等待审核和合并

### 检查点
- 在提交前检查当前分支
- 确保不在 `develop` 或 `main` 分支上直接提交
- 提醒用户创建功能分支