---
description: Database migration, alembic, models, schema changes, 数据库, 迁移
globs: ["backend/app/models/**/*", "backend/migrations/**/*", "scripts/db/**/*", "deployment/scripts/db*.sh"]
---
# 数据库管理规范

## 环境差异

### Local 环境
- **Python 环境**: ✅ 有虚拟环境
- **Docker 环境**: ✅ 可选
- **推荐工具**: `./scripts/db/alembic.sh`
- **环境文件**: `deployment/environments/env.local`

### 服务器环境 (dev/stg/prod)
- **Python 环境**: ❌ 无
- **Docker 环境**: ✅ 必需
- **推荐工具**: `./deployment/scripts/db-docker.sh`
- **环境文件**: `deployment/environments/env.{dev|stg|prod}`

## 数据库迁移操作

### Local 环境命令

```bash
cd backend
source .venv/bin/activate

# 检查当前状态
./scripts/db/alembic.sh local current

# 查看迁移历史
./scripts/db/alembic.sh local history

# 应用所有迁移
./scripts/db/alembic.sh local upgrade

# 生成新迁移
./scripts/db/alembic.sh local autogen "add new feature"

# 降级到上一版本
./scripts/db/alembic.sh local downgrade
```

### 服务器环境命令

```bash
# Dev 环境
./deployment/scripts/db-docker.sh dev current
./deployment/scripts/db-docker.sh dev upgrade
./deployment/scripts/db-docker.sh dev autogen "add new feature"

# Staging 环境
./deployment/scripts/db-docker.sh stg upgrade
./deployment/scripts/db-docker.sh stg current

# Production 环境（谨慎操作）
./deployment/scripts/db-docker.sh prod upgrade
./deployment/scripts/db-docker.sh prod current
```

## 数据库结构修改流程

**绝对禁止直接执行 SQL DDL 命令**

1. **修改模型文件** - 在 `backend/app/models/` 目录下修改相应的 SQLAlchemy 模型
2. **生成 Alembic 迁移文件** - 执行迁移命令
3. **执行迁移** - 执行 `upgrade` 命令

### 迁移文件管理
- 迁移文件位于 `backend/migrations/versions/` 目录
- 每次修改数据库结构都必须生成新的迁移文件
- 迁移文件一旦提交到版本控制，不得修改
- 如果需要修改，创建新的迁移文件

## 数据库访问优先级

1. **优先使用** MCP postgres-supplynexus-local 工具（如果可用）
2. **备选方案** Docker 容器访问
3. **最后选择** Python 脚本（避免创建临时测试文件）

## 安全注意事项

### 迁移安全
1. **生产环境**: 迁移前必须备份数据库
2. **测试环境**: 先在 staging 环境测试迁移
3. **回滚准备**: 确保可以快速回滚到上一版本

### 环境隔离
1. **数据隔离**: 不同环境使用不同的数据库
2. **配置隔离**: 每个环境有独立的环境文件
3. **权限隔离**: 生产环境使用最小权限原则

## 故障排除

### 连接失败
```bash
# 检查数据库服务状态
docker-compose ps postgres

# 检查网络连接
docker network ls
```

### 迁移冲突
```bash
# 查看迁移历史
alembic history

# 检查迁移文件
cat backend/migrations/versions/latest_migration.py
```

### 紧急恢复
```bash
# 回滚到上一版本
alembic downgrade -1

# 强制标记版本
alembic stamp <revision_id>
```
